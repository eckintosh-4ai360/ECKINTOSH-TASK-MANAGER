# Auth.js 5 beta declares an outdated optional Nodemailer peer range. The app
# uses Nodemailer 10 for its security fixes; keep installs reproducible until
# Auth.js publishes a compatible peer range.
legacy-peer-deps=true
